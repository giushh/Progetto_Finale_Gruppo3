### RUN FRONTEND come MODULO ###  
python3 -m streamlit run FRONTEND/app.py

### freeze dei requirements ###
pip freeze > requirements.txt

### freeze dei requirements con pipreqs (effettivamente utilizzati nella repo) ###
pipreqs . --ignore .venv --savepath requirements.txt
 
## Installazione requirements ##
pip install -r requirements.txt

## Creazione e arttivazione venv ##
python -m venv .venv
. .venv/Scripts/activate



